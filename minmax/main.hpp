#pragma once
#include <utility>

// implement your min(x, y) and max(x, y) functions here.
// - the functions take the arguments by reference
// - the functions return the arguments by reference
// - if the advanced version is enabled: functions are able to take and return rvalues by ref.
